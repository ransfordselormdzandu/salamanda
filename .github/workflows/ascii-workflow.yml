name: ASCII Workflow

on:
  - push

jobs: 
    first_job: 
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v6

            - name: Execute shell script
              run: |
                chmod +x ascii-script.sh
                ./ascii-script.sh

